# Repo Notes

- This repo checks in generated action bundles under `dist/`.
- When changing source used by an action entrypoint, rebuild the affected `dist/*` output and commit it in the same change.
